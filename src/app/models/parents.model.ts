export interface Parents {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    phone: string;
    children: string[]; // liste des identifiants des enfants associés
  }
  